import localFont from "next/font/local";

export const mallory = localFont({
  src: "./fonts/Mallory.otf",
  variable: "--font-mallory",
  display: "swap",
});

export const goudyIni = localFont({
  src: "./fonts/GoudyIni.ttf",
  variable: "--font-goudy",
  display: "swap",
});
