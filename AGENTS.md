# This is NOT the Next.js you know

- Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

- ALWAYS suggest a commit head message after done with a task if changes were done, a message that is human-readable and spicific and copyable.

- don't unnecessarily run `npm run build` or `npm run lint` command for small stuff unless needed.

- this is a real site set for production, don't mimick coding or use tricks to hide problems, do real correct coding like a real software engineer.

- When asked to make a change in ui that can be done in a minute, do it instantly without chatty confirmations or extra questions, just do it.

- don't over-engineer, solve the problem simply and directly, don't create unnecessary abstractions or layers of indirection.