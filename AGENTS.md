# This is NOT the Next.js you know

- Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

- always suggest a commit head message after done with a task if changes were done, a message that is human-readable and spicific.

- don't unnecessary keep running build command for easy stuff and using resources unless needed